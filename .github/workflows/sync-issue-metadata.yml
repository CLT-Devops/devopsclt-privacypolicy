name: Sync Issue Metadata

on:
  issues:
    types:
      - opened

permissions:
  contents: read
  issues: write

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    uses: CLT-Devops/.github/.github/workflows/sync-issue-metadata.yml@main
